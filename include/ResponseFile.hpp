#ifndef RESPONSEFILE_HPP
#define RESPONSEFILE_HPP

#include <string>

namespace hhpp {
	class ResponseFile
	{
	public:
		ResponseFile();
		~ResponseFile();

		void filename(std::string filename);

	private:

	};
}

#endif
